import { CreateUserForm } from "@/components/auth/signup/CreateUserForm";
import React from "react";

const page = () => {
  return (
    <main className="flex justify-center">
      <CreateUserForm />
    </main>
  );
};

export default page;